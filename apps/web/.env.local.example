# Copy to .env.local for running `npm run dev` directly on the host
# (outside Docker). Point this at wherever the gateway is actually
# reachable - defaults match infra/docker/docker-compose.yml's host ports.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8090/api/v1
